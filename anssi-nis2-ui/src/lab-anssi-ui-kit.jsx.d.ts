// Fichier créé manuellement, car depuis l'UI Kit v1.26, il n'est plus généré automatiquement.
// En théorie, l'UI kit devrait le fournir, mais comme solution de contournement, on le crée manuellement.
declare namespace JSX {
  interface IntrinsicElements {
    "lab-anssi-centre-aide": {
      nomService: string;
      liens: string;
    };
    "lab-anssi-bouton-suite-cyber-navigation": {
      sourceUtm?: string;
    };
    "lab-anssi-navigation-pied-de-page": {
      conforme?: boolean;
    };
    "lab-anssi-mes-services-cyber-bandeau": object;
    "lab-anssi-resume-pssi": {
      nomService: string;
    };
    "lab-anssi-presentation-anssi": object;
  }
}
